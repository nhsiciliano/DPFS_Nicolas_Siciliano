<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head', { title: 'Registro' , css: 'auth' }) %>

    <body>
        <%- include('../partials/header') %>

            <main class="container auth-container">
                <div class="auth-card">
                    <h1>Crear Cuenta</h1>
                    <p class="auth-subtitle">Únete a PetSpace y disfruta de beneficios exclusivos</p>

                    <form action="/users/register" method="POST" class="auth-form" enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="firstName">Nombre</label>
                            <input type="text" id="firstName" name="firstName" placeholder="Ej. Juan"
                                value="<%= locals.oldData ? oldData.firstName : '' %>" required>
                        </div>
                        <div class="form-group">
                            <label for="lastName">Apellido</label>
                            <input type="text" id="lastName" name="lastName" placeholder="Ej. Pérez"
                                value="<%= locals.oldData ? oldData.lastName : '' %>" required>
                        </div>

                        <div class="form-group">
                            <label for="email">Correo Electrónico</label>
                            <input type="email" id="email" name="email" placeholder="ejemplo@correo.com"
                                value="<%= locals.oldData ? oldData.email : '' %>" required>
                            <% if (locals.errors && errors.email) { %>
                                <div class="text-danger" style="color: red; font-size: 0.8rem; margin-top: 5px;">
                                    <%= errors.email.msg %>
                                </div>
                                <% } %>
                        </div>

                        <div class="form-group">
                            <label for="password">Contraseña</label>
                            <input type="password" id="password" name="password" placeholder="Mínimo 8 caracteres"
                                required>
                        </div>

                        <div class="form-group">
                            <label for="image">Foto de Perfil</label>
                            <input type="file" id="image" name="image" accept="image/*">
                        </div>

                        <div class="form-group checkbox-group">
                            <input type="checkbox" id="terms" name="terms" required>
                            <label for="terms">Acepto los <a href="#">Términos y Condiciones</a></label>
                        </div>

                        <button type="submit" class="btn btn-primary btn-block">Registrarse</button>
                    </form>

                    <div class="auth-footer">
                        <p>¿Ya tienes una cuenta? <a href="/users/login">Inicia Sesión</a></p>
                    </div>
                </div>
            </main>

            <%- include('../partials/footer') %>
    </body>

</html>